[request_definition]
r = sub, obj, act

[policy_definition]
p = sub, obj, act, eft

[role_definition]
g = _, _

[policy_effect]
e = some(where (p.eft == allow))

[matchers]
# 简单匹配：如果 policy 与请求匹配则允许
m = g(r.sub, p.sub) && URIMatch(r.obj, p.obj) && (r.act == p.act || p.act == "*")
